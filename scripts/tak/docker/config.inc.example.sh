## Definable variables
#  Copy this file to `config.inc.sh`
#
# These Defaults work; you can change them as needed
#
#
export CAPASS="atakatak"               # CA Password [not fully tested if a change works]
export CERTPASS="atakatak"             # User Cert Password [not fully tested if a change works]
export DATABASE_ALIAS="tak-database"   # Docker network alias
export DOCKER_SUBNET="172.20.0.0/24"   # Docker subnet
export TAK_COT_PORT=8089               # TAK API Port [not fully tested if a change works]
export TAK_CLIENT_VALID=30             # Days Client Cert is valid
export TAKADMIN=tak-admin              # TAK Web Admin

